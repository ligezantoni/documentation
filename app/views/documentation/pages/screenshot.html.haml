= form_for @screenshot, :url => upload_screenshot_path, :html => {:class => 'pageForm'} do |f|
  %dl
    %dt= f.label :upload_file
    %dd.padded
      - if params[:filename]
        = params[:filename]
      - else
        = f.file_field :upload_file

  %dl
    %dt= f.label :alt_text
    %dd= f.text_field :alt_text

  %p.submit
    = f.submit "Upload Screenshot", :class => 'button'